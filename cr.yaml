# Configuration for helm/chart-releaser-action
# See: https://github.com/helm/chart-releaser-action

# The branch that hosts the Helm index (created automatically on first run).
pages-branch: gh-pages

# charts-repo-url defaults to https://<owner>.github.io/<repo>
# Uncomment and set explicitly if needed:
# charts-repo-url: https://Gasket-Gateway.github.io/helm
